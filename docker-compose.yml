services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hermione
      POSTGRES_PASSWORD: hermione
      POSTGRES_DB: hermione
    ports:
      - "5432:5432"
    volumes:
      - hermione_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hermione"]
      interval: 2s
      timeout: 3s
      retries: 15

volumes:
  hermione_pgdata:
